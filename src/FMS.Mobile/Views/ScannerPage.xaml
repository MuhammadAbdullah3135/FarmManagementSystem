<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="FMS.Mobile.Views.ScannerPage"
             Title="Scan Animal Tag">

    <Grid>
        <!-- Camera preview -->
        <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
            IsDetecting="True"
            BarcodesDetected="OnBarcodesDetected" />

        <!-- Overlay UI -->
        <VerticalStackLayout VerticalOptions="End" Padding="20" Spacing="12">
            <!-- Manual entry fallback -->
            <Frame BackgroundColor="White" CornerRadius="12" Padding="16" Opacity="0.95">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Or enter tag manually:" FontSize="13" TextColor="Gray" />
                    <HorizontalStackLayout Spacing="8">
                        <Entry x:Name="ManualTagEntry"
                               Placeholder="Tag number"
                               HorizontalOptions="FillAndExpand"
                               ReturnCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=OnManualEntryCommand}" />
                        <Button Text="Go" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=OnManualEntryCommand}"
                                BackgroundColor="{StaticResource Primary}" TextColor="White"
                                WidthRequest="60" CornerRadius="8" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Close button -->
            <Button Text="Cancel" BackgroundColor="Gray" TextColor="White"
                    CornerRadius="8" Clicked="OnCancelClicked" />
        </VerticalStackLayout>
    </Grid>

</ContentPage>
