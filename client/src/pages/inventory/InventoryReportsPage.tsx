import React, { useCallback, useEffect, useState } from 'react';
import { Alert, Button, Card, Col, Row, Space, Statistic, Table, Tag } from 'antd';
import { DownloadOutlined, ReloadOutlined } from '@ant-design/icons';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { ColumnsType } from 'antd/es/table';
import { inventoryApi } from '../../api/inventory';
import { getApiError } from '../../api/farmApi';
import type { InventoryLowStockAlert, InventoryReport, InventoryStockReportItem } from '../../types';

const downloadCsv = (filename: string, rows: string[][]) => {
  const csv = rows.map(row => row.map(value => `"${value.replace(/"/g, '""')}"`).join(',')).join('\n');
  const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv;charset=utf-8;' })); link.download = filename; link.click(); URL.revokeObjectURL(link.href);
};

const InventoryReportsPage: React.FC = () => {
  const [report, setReport] = useState<InventoryReport | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { setLoading(true); setError(null); try { setReport((await inventoryApi.report()).data); } catch (err) { setError(getApiError(err)); } finally { setLoading(false); } }, []);
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [load]);
  const exportStock = () => { if (!report) return; downloadCsv('inventory-stock.csv', [['Item', 'Category', 'Unit', 'Quantity', 'Reorder Level', 'Unit Cost', 'Stock Value', 'Location'], ...report.currentStock.map(i => [i.itemName, i.category || '', i.unit, `${i.quantity}`, `${i.reorderLevel}`, `${i.unitCost}`, `${i.stockValue}`, i.location || ''])]); };
  const stockColumns: ColumnsType<InventoryStockReportItem> = [{ title: 'Item', dataIndex: 'itemName' }, { title: 'Category', dataIndex: 'category', render: (v?: string) => v || '-' }, { title: 'Quantity', dataIndex: 'quantity', render: (v: number, r) => <span>{v} {r.isLowStock && <Tag color="error">Low</Tag>}</span> }, { title: 'Reorder level', dataIndex: 'reorderLevel' }, { title: 'Unit cost', dataIndex: 'unitCost', render: (v: number) => `$${v.toFixed(2)}` }, { title: 'Stock value', dataIndex: 'stockValue', render: (v: number) => `$${v.toFixed(2)}` }, { title: 'Location', dataIndex: 'location', render: (v?: string) => v || '-' }];
  const alertColumns: ColumnsType<InventoryLowStockAlert> = [{ title: 'Item', dataIndex: 'itemName' }, { title: 'Quantity', dataIndex: 'quantity', render: (v: number, r) => `${v} ${r.unit}` }, { title: 'Reorder level', dataIndex: 'reorderLevel' }, { title: 'Shortfall', dataIndex: 'shortfall', render: (v: number, r) => <strong style={{ color: '#cf1322' }}>{v} {r.unit}</strong> }, { title: 'Location', dataIndex: 'location', render: (v?: string) => v || '-' }];
  return <Space direction="vertical" size="large" style={{ width: '100%' }}>{error && <Alert type="error" showIcon message={error} closable onClose={() => setError(null)} />}<Card title="Inventory Reports" extra={<Space><Button icon={<ReloadOutlined />} onClick={() => void load()}>Refresh</Button><Button icon={<DownloadOutlined />} onClick={exportStock} disabled={!report}>Export stock CSV</Button></Space>} loading={loading}><Row gutter={16}><Col xs={24} sm={8}><Statistic title="Total stock value" value={report?.totalStockValue ?? 0} precision={2} prefix="$" /></Col><Col xs={24} sm={8}><Statistic title="Inventory items" value={report?.totalItems ?? 0} /></Col><Col xs={24} sm={8}><Statistic title="Low-stock alerts" value={report?.lowStockItemCount ?? 0} valueStyle={{ color: report?.lowStockItemCount ? '#cf1322' : undefined }} /></Col></Row></Card><Card title="Movement volume"><ResponsiveContainer width="100%" height={260}><BarChart data={report?.movementSummary ?? []}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="movementTypeName" /><YAxis /><Tooltip /><Bar dataKey="quantity" fill="#1677ff" name="Quantity" /></BarChart></ResponsiveContainer></Card><Card title="Low-stock alerts" extra={<Tag color={report?.lowStockItemCount ? 'error' : 'success'}>{report?.lowStockItemCount ?? 0} items</Tag>}><Table rowKey="inventoryItemId" columns={alertColumns} dataSource={report?.lowStockAlerts ?? []} pagination={false} locale={{ emptyText: 'All inventory items are above their reorder levels.' }} /></Card><Card title="Current stock" extra={<Button icon={<DownloadOutlined />} onClick={exportStock}>Export CSV</Button>}><Table rowKey="inventoryItemId" columns={stockColumns} dataSource={report?.currentStock ?? []} pagination={{ pageSize: 10 }} /></Card></Space>;
}; export default InventoryReportsPage;
